import React, { useState } from "react";
import { storesDummyData } from "../assets/assets";
const Seller = () => {
  const storeData = useState(storesDummyData);

  return <div className="inset-0"></div>;
};

export default Seller;
